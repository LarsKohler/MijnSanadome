{
  "name": "sanadome-hrms-login",
  "version": "1.0.0",
  "description": "HRMS login voor Sanadome Hotel & Spa",
  "private": true,
  "scripts": {
    "setup:local": "node scripts/setup-local-config.js",
    "build": "node scripts/build.js",
    "dev": "npm run setup:local && npx serve ."
  }
}
